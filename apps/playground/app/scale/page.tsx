import { SpacingStudio } from "../../components/scale/SpacingStudio";

/** The scale studio: spacing today, radius and elevation to follow. */
export default function ScalePage() {
  return <SpacingStudio />;
}
